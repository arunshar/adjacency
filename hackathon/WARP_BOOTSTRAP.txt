You are taking over an in-progress project in /Users/arunsharma/code/adjacency. Read
WARP.md and hackathon/WARP_HANDOFF.md in full before you respond. Where any other
document disagrees with the handoff, the handoff wins. Where the repository disagrees
with the handoff, the repository wins and you report the difference.

REFRESHED 2026-08-06T10:35Z. The previous version of this file told you to expect
branch main at cd218dd with 448 passed and 2 xfailed, and to stop if anything differed.
All of that is now wrong, and following it would have halted you on arrival. If you are
reading a checkpoint that does not match section VERIFY below, trust section VERIFY.

THE PROJECT. Adjacency is a brand-safety policy engine: advertiser prose compiles into
a typed, hashed PolicySpec, inventory is evaluated, and every verdict passes through
deterministic fail-closed gates G0 to G6. ImagineSignal extends the same discipline to
creative generation: one approved ad image becomes a small family where exactly one
declared visual attribute changes and everything else stays locked and byte-verified,
with gates IS0 to IS8 and an append-once decision receipt. The output is not an image,
it is a decision: keep, edit, test, hold, review, or stop, with the evidence class that
justified it attached.

FIVE HARD RAILS. These lead because you have a terminal and can execute every one of
the forbidden things.

1. Never commit, stage, push, tag, open a pull request, reset, clean, stash, or discard.
   Arun runs every git write himself. git status, git diff, and git log are fine.
   Note the change from the last version: the tree is no longer dirty, the work is
   committed and pushed. The rail is unchanged. The reason is now release hygiene on a
   branch with a live pull request rather than protecting uncommitted work.
2. Never make a live xAI or X Ads call. Never read XAI_API_KEY, never set ADJ_RECORD,
   never pass --write. Arun runs hackathon/smoke_call.py himself, not you.
3. Never add retry or fallback to the Imagine transport, and never fall back from replay
   to a live call. A missing fixture is terminal.
4. Never record a cost of zero for an unreported provider cost. Zero is a value the
   provider reports. An absent cost is CostMeasurement.unknown() and it deliberately
   blocks further calls.
5. Plain ASCII hyphens only. Never an em dash or an en dash, anywhere.

Also: use python3.13, never bare python3. The system interpreter is 3.14.6 and
pyproject.toml requires >=3.12,<3.14.

FILES YOU MUST NOT WRITE. Nothing catches a mistake in these, because they are the thing
doing the catching: gates.py, decisions.py, contracts.py, canonical.py, receipts.py,
mutations.py and ports.py in src/adjacency/imagine_signal/, plus src/adjacency/gates.py,
src/adjacency/contracts.py, and the cost accounting inside imagine_client.py. Everything
else is delegatable because the gates, the suite, and the artifact digest catch errors.

HONESTY SPINE. Evidence classes are UNIT_TESTED, FROZEN_REPLAY, and SIMULATED.
Production is NO_GO and the action ceiling is TEST. Never claim, in code, comments,
documentation, or UI text: live Grok image quality, image-quality lift, advertiser lift,
causal lift, production readiness, or X revenue of any kind.

VERIFY THIS CHECKPOINT BEFORE EDITING ANYTHING:

  cd /Users/arunsharma/code/adjacency
  git branch --show-current && git rev-parse --short HEAD && git status --short
  env -u XAI_API_KEY -u ADJ_RECORD .venv/bin/pytest -m "not e2e" -q
  env -u XAI_API_KEY -u ADJ_RECORD .venv/bin/python -I -B scripts/run_imagine_signal_demo.py --verify-only
  shasum -a 256 artifacts/imagine_signal/offline_demo.json
  ./hackathon/verify_tree.sh

Expected:

  branch                   canary/imagine-signal
  git status --short       prints NOTHING
  HEAD vs origin           identical, and 0 ahead 0 behind
  suite                    488 passed, no xfails
  demo                     "status": "verified", network_used: false
  artifact digest          b17e9105f0de89772440c82938a69a7b25452784fc9c0667a4fe81a8f621ca73
  main                     cd218dd, locally and on origin
  verify_tree.sh           no core-file STOP banner

A NOTE ON WHY NO COMMIT SHA IS LISTED. An earlier version of this file pinned an exact
HEAD and told you to stop on any mismatch. It went stale on the very next commit, which
was ordinary correct work, and a stale checkpoint that says "stop" is a landmine rather
than a guard. The properties above survive commits. HEAD is expected to move. If you
want the SHA the release was cut at, the tag carries it.

Judge a mismatch by what it means, not by whether it differs:

  HEAD moved, tree clean, suite green      normal. Someone committed. Continue
  tree dirty, and you did not edit it      report it and stop. Something else is running
  suite below 488, or digest changed       report it and STOP. Do not repair, and never
                                           regenerate an artifact to make a digest match
  main is not cd218dd                      report it and STOP. main is the fallback

Two details that look like bugs and are not. The tag v0.3.0-canary points at 53ad8d4,
behind HEAD, because the release was tagged before later documentation commits landed.
That is correct, leave it. And main sitting several commits behind is the plan: it is the
known-good fallback and it never moves during the event.

WHAT IS ALREADY DONE. Do not rebuild any of this.

  Live xAI transport   Implemented, using inline response_format=b64_json. Confirmed by
                       9 real probes costing $0.199 total. The URL-fetch path it replaced
                       is dead code, still present, and still imported by 4 tests.
  Demo surface         Built. app.py serves an ImagineSignal tab beside Autopsy, 496
                       lines in src/adjacency/imagine_signal/ui.py, 5 tests.
  Canary release       Branch pushed, tag v0.3.0-canary pushed, draft pull request #10
                       open, CI green on both jobs.

Every task in the old WARP_TASKS.md queue is complete. Read the current
hackathon/WARP_TASKS.md for what actually remains, which is short.

THREE THINGS THAT ARE NOT WHAT YOU MIGHT ASSUME. The xAI Grokathon is Saturday
2026-08-08, and the organizer confirmed the brief is assigned that morning, so
ImagineSignal is probably not the submission. There is no scheduled interview at the
event. And .github/workflows/ci.yml triggers only on pull_request and on push to main,
so pushing a branch runs zero checks: a green branch means nothing until a pull request
exists. That trap already cost one failed pull request, #9, which was closed and its
branch deleted, which hid the failure from every ordinary view.
hackathon/CONTINGENCY.md has the 09:00 decision tree.

YOUR FIRST MOVE. Run the checkpoint commands. Read hackathon/WARP_TASKS.md. Then
summarize back in about eight lines: what this project is, the checkpoint result, which
rails constrain you, and which task you are taking first. Then either start that task or
ask one specific question. Do not ask for permission in general terms.
